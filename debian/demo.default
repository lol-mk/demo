# Defaults for demo initscript
# sourced by /etc/init.d/demo
# installed at /etc/default/demo by the maintainer scripts

USERID="nobody"

#
# This is a POSIX shell fragment
#

# Additional options that are passed to the Daemon.
DAEMON_OPTS="-q -c /etc/demo/config.json"
